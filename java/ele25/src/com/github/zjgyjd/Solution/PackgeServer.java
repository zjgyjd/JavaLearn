package com.github.zjgyjd.Solution;

import java.util.Scanner;

public class PackgeServer {
    public static void main(String[] args) {
        //进行对输入进行处理
        Scanner s = new Scanner(System.in);
        //
        while(s.hasNext()){
            String[] news = s.nextLine().split(" ");//得到1 2
            //将信息存入数组中
            int row= Integer.parseInt(news[1]);
            while( row != 0){
                row--;

            }
        }
    }
}
